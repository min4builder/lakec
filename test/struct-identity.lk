type a struct (
	a, b, c int,
	etc mut []char,
);

type b struct (
	d, e, f int,
	etc mut []char,
);

_ struct c (
	g, h, i int,
	blah mut []char,
);

f() void {
	auto d *c = &[c](0);
	auto e *a = d;
	auto f *b = e;
	auto g *c = f;
}

