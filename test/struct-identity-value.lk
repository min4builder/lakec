typedef a struct (
	a, b, c int,
	etc mut []char,
);

typedef b struct (
	d, e, f int,
	etc mut []char,
);

struct c (
	g, h, i int,
	blah mut []char,
);

f() void {
	auto d struct c = (0);
	auto e a = d;
	auto f b = e;
	auto g struct c = f;
}

